# TCP Latency v0.12.0

## HUD 背景模式

- 新增 **毛玻璃 / 纯透明 / 实体背景** 三种悬浮 HUD 背景样式。
- 默认继续使用 macOS 原生毛玻璃，兼容现有视觉效果。
- 纯透明模式会同时关闭 AppKit `UnderWindowBackground` 与 CSS 卡片背景、边框和阴影，只保留网络猫、延迟、实时流量、目标名称与状态点。
- 实体背景模式关闭原生模糊，使用随 Light / Dark 自动适配的高对比表面。
- 设置页新增“背景样式”选择；纯透明模式下背景强度滑块自动禁用。

## 配置与兼容性

- 新增 `floatingBackgroundMode` 配置字段，支持 `glass` / `transparent` / `solid`。
- 旧配置自动保持 `glass` 默认值，无需用户重新设置。
- Rust 侧增加背景模式校验与测试。
- macOS 原生窗口继续保持透明、无矩形系统阴影，并按背景模式动态启用或清除窗口特效。

## Release

- 版本号更新为 `0.12.0`。
- Tag：`v0.12.0`。
